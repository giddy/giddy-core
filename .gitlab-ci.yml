image: frekele/gradle

services:
  - postgres:latest

variables:
  POSTGRES_DB: giddy
  POSTGRES_USER: runner
  POSTGRES_PASSWORD: ""

cache:
  paths:
    - .gradle/

build:
  script: "gradle clean build --debug"